pip install django 
python.exe -m pip install --upgrade pip 
python -m venv venv 
venv\Scripts\activate
git init
pip install google-auth-oauthlib google-api-python-client
pip install opencv-python 
pip install numpy 
python -m pip install Pillow

python manage.py check  



git

git add . (Para preparar los cambios nuevos).

git commit -m "Explicación breve de lo que cambiaste" (Para sellar el paquete).

git push (Para mandarlo a GitHub). Ya no necesitas poner -u origin main porque Git ya se aprendió el camino.
